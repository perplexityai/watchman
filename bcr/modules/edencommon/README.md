# edencommon

Hand-maintained Bazel overlay for the EdenCommon snapshot used by Watchman.

The overlay exposes only utilities and telemetry libraries required by
Watchman. Update `overlay/BUILD.bazel` when the pinned upstream snapshot
changes.
